import styled from 'styled-components'
import DauntlessSquare from '../dauntlessSquare'

const DauntlessButton = styled(DauntlessSquare)`
  font-family: inherit;
  color: inherit;
  font-weight: inherit;
  text-shadow: inherit;
  font-size: inherit;
`

export default DauntlessButton